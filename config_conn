<?php

$servern = "localhost";
$usern = "root";
$passn = "mypassword";
$dbname = "mydb";

// Create a connection within your server to database
$myconn = mysqli_connect($servern, usern, $passn, $dbname);

// Check connection
If (!$conn){
  die("Connection failed: " . mysqli_connect_error);
}
  echo "Connected Successfully";

?>
